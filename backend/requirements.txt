# Core Django & REST
django>=4.2
djangorestframework
django-cors-headers

# Database & production helpers
psycopg2-binary
dj-database-url
gunicorn
whitenoise[brotli]

# Existing / common deps (add if you see them missing after pip install)
django-filter
drf-yasg                # if swagger is used
python-dotenv           # optional but useful
djangorestframework-simplejwt>=5.3.0
drf-writable-nested>=0.7.0